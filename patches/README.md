# libfprint patch series

Record the exact upstream base in `UPSTREAM_COMMIT` and keep numbered
`git format-patch` output here once libfprint driver development begins.

Do not vendor the complete upstream source tree or raw capture data.
